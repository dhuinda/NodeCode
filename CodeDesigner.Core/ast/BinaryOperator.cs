namespace CodeDesigner.Core.ast;

public enum BinaryOperator
{
    // LT = Less Than, GT = Greater Than, LE = Less than or Equal to, ..., EQ = EQual, NE = NotEqual
    PLUS, MINUS, TIMES, DIVIDE, LT, GT, LE, GE, EQ, NE, MODULO, AND, OR

}